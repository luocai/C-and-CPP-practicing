中缀表达式化为后缀表达式，主要思路为：建一个操作符栈，遍历表达式，当str[i]为0到9的字符时，则直接输出
(加入辅助字符串的末尾），当str[i]为操作符时，栈为空或者为“（”则直接入栈，否则就判断str[i]的优先级是否
不大于栈顶符号的优先级，是的话就循环输出并弹出栈中的元素直到栈为空或者不满足mp[str[i]] <= mp[s.top()]时
退出循环，继续遍历，否的话就把str[i]入栈。若str[i]为右括号，则循环输出栈中元素直到遇见左括号。特别注意的是弹完括号内所有元素后
要把左括号（ 也弹出去。遍历完表达式后把栈内所有元素全部输出就得到后缀表达式啦！